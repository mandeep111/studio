rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow any authenticated user to read and write to storage.
    // This is for debugging purposes. We can tighten these rules later.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
