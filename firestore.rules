rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read on all documents for development purposes.
    // For production, you should restrict this further.
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}